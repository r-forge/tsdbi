

# for Windows I need to provide Makevars.win 
case `uname` in
  SunOS)
    LD_LIBS=-lm -lc -lnsl
    ;;
  Linux)
    LD_LIBS=-L/usr/local/lib
    ;;
esac

echo "s:@LD_LIBS@:$LD_LIBS:"
sed -e "s:@LD_LIBS@:$LD_LIBS:" <src/Makevars.in > src/Makevars
